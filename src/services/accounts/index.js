const { create } = require('./create');
const { get } = require('./get');
const { updateActive } = require('./updateActive');
const { updateBlocked } = require('./updateBlocked');

module.exports = {
  create,
  get,
  updateActive,
  updateBlocked,
};
